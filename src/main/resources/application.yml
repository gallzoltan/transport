transports:
  incomeDeclinePercentage:
    for30min: 0
    for60min: 10
    for120min: 20
    from120min: 30
spring:
  mvc:
    format:
      date: yyyy-MM-dd
      time: HH:mm:ss
      date-time: yyyy-MM-dd HH:mm:ss
  datasource:
    url: jdbc:postgresql://0.0.0.0:5432/transport
    username: gallz
    password: gzaltf1
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    open-in-view: false